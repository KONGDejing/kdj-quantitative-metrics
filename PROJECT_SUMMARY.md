# 项目总纲：KDJ 量化盯盘与交易计划辅助系统

> 最近核对：2026-08-26
> 用途：恢复工程上下文、说明真实实现边界、记录后续开发优先级。
> 动态持仓与成交不写入本公开文档，以本机 `config.yaml`、交易记忆和用户最新确认内容为准。

## 一、项目定位

本项目已经从最初的“KDJ 超买/超卖邮件提醒器”演进为三部分：

1. **行情监控**：在 A 股交易时段获取行情、计算 KDJ、展示 K 线并发送提醒。
2. **策略研究**：运行 KDJ 回测、个股阈值寻优和价格波段 B/S 分析。
3. **个人交易计划**：保存人工成交流水，生成收盘总结和次日 T+1 操作指引。

系统始终只做提醒和决策辅助，不连接券商、不自动下单。

长期目标是让中航光电策略在真实持仓、资金和风险约束下争取年化 20%，理想目标 30%，并通过机械执行降低追涨、跟风和情绪交易。该目标只针对中航光电策略，不是 50 万元总账户的收益承诺。正式绩效口径按约 20 万元策略资金计算并包含等待机会的现金，同时单独展示实际持仓收益率、最大回撤和样本外表现。

## 二、当前真实架构

```text
AkShare/东方财富 ─┐
                  ├→ data_provider → KDJ → AppState(内存) → Web/API
新浪降级/本地缓存 ┘                       ├→ 阈值提醒
                                           ├→ 14:50 总结
Web 人工成交 → opening + trade_history → trade_ledger → decision_engine → 15:15后确定性主计划
交易记忆 + 正式日线 ───────────────────────────────────┘          ├→ LLM只读复核（单独发送）
                                                                  └→ shadow_tracker → 到期评分

长历史数据 → walk_forward状态分层 → stage_research分手数资金曲线 → 阶段晋级闸门
写接口 → X-API-Key → 成交副本重放校验 → 只保存最终正确事实
交易日历 + runtime_state → 调度、分通道通知幂等与重启恢复
交易记忆 + config 持仓 → runner → 可选 LLM 建议
```

### 后端模块

| 模块 | 当前职责 |
| --- | --- |
| `src/api.py` | FastAPI 生命周期、静态页面和 REST API |
| `src/runner.py` | 监控调度、`1d_est`、阈值提醒、收盘总结、次日指引 |
| `src/data_provider.py` | 实时 K 线、数据源降级、回测长历史和 CSV 缓存 |
| `src/kdj.py` / `strategy.py` | KDJ(9,3,3) 与高低区间信号 |
| `src/state.py` | 观察列表、内存状态、提醒去重、人工成交写入 |
| `src/trade_ledger.py` | 从起始仓位和流水重放核心/T仓、双成本、已实现收益与T+1状态 |
| `src/decision_engine.py` | 正式日线确认、资金/仓位/回撤/基本面闸门、结构化动作和取消条件 |
| `src/reverse_t_engine.py` | 简单冲高反T、备用趋势开关、20%额度、只在盈利价回补 |
| `src/performance_store.py` | 从真实成交回放完整策略资金池每日净值、高水位和路径回撤 |
| `src/shadow_tracker.py` | 冻结正式计划，按5/10/20/30/60交易日评价增量动作，并标注无前视价格状态 |
| `src/walk_forward.py` | 扩展训练窗口、后续测试窗口及按价格状态分组的独立超卖事件研究 |
| `src/stage_research.py` | 将不重叠样本外事件转换为10/15/20/30/40手完整资金池曲线和晋级检查 |
| `src/runtime_state.py` | 持久化提醒、冷却、区间、分通道任务结果和不含错误值的纠错审计元数据 |
| `src/trading_calendar.py` | 依据交易所年度休市安排判断交易日，未知年份默认停止任务 |
| `src/auth.py` | 生成/校验本地写入令牌，保护全部写接口 |
| `src/notifier.py` | SMTP 邮件和 Pushplus 多 token 通知 |
| `src/backtest.py` | 服务内 KDJ 全仓开平策略回测 |
| `src/optimizer.py` | 25 组 KDJ 阈值网格扫描与持久化 |
| `src/band_analysis.py` | 价格到 B 买、到 S 卖的区间搜索和明细模拟 |
| `src/llm_advisor.py` | 复用ChatGPT登录态的Codex只读主通道、Axera备用通道、结构化结果校验和健康检查 |

### 前端

`web/` 是无构建步骤的原生单页，提供：

- 观察标的增删切换；
- 日线、10 分钟线和 `1d_est` 最新 KDJ；
- SVG K 线图和个股阈值标记；
- 当日/历史提醒；
- 人工成交录入；
- 成交ID查询、最终值纠正和错误记录删除；
- KDJ 回测、自动寻优结果和价格波段分析。

页面每 10 秒请求一次 `/api/status`。

## 三、运行与数据状态

### 实时监控

- 交易时段：工作日 9:30–11:30、13:00–15:00，收盘宽限 90 秒。
- 轮询：默认 60 秒。
- 当前本机观察池：沪深300、中航光电、烽火通信；以 `config.yaml` 为准。
- 当前独立到价观察：长电科技71元、烽火通信39元、川环科技25元，均为空仓候选1手；
  进入目标价上方0.5%区域时提醒，行情超过3分钟或日期不是当天则不发送。
- 当前本机 Web 端口：`8311`；仓库模板仍使用 `8010`，部署时以 `web.port` 为准。
- 当前服务由 `systemd --user` 托管，代码或配置修改后必须重启。

### 状态持久化边界

| 数据 | 保存位置 | 重启后 |
| --- | --- | --- |
| 观察列表、交易计划、人工成交流水 | `config.yaml` | 保留 |
| KDJ、K线序列 | 进程内存 | 启动后自动重新填充 |
| 提醒列表、提醒区间和冷却 | `runtime/app_state.json` | 保留 |
| 每日总结/次日指引分通道结果 | `runtime/app_state.json` | 保留，成功通道不重复发送 |
| 日线历史缓存 | `cache/daily_*.csv` | 保留 |
| 个股寻优结果 | `best_params.json` | 保留 |
| 日志 | `logs/` 和 journald | 保留/轮转 |
| 交易偏好与上下文 | 外部持久化记忆目录 | 保留 |
| 策略净值、影子决策、阶段资金曲线与walk-forward报告 | `runtime/` | 保留，不提交 Git |

### 真实持仓的判定顺序

1. 用户最新明确确认；
2. `trade_plan.positions.*.opening` + `trade_history` 由 `trade_ledger` 逐笔重算；
3. `last_report`；
4. `base_lots`、`base_lots_remaining`、`t_lots_held`、`cost` 等汇总字段。

股票代码可能被 YAML 解析成数字，匹配时必须使用 `str(key) == code`。

## 四、提醒和交易指引规则

### KDJ 阈值提醒

- 全局默认阈值：`K <= 20` 低位、`K >= 80` 高位。
- 有 `best_params.json` 时，按个股寻优阈值覆盖全局阈值。
- 同一股票同一区域连续停留不重复提醒；回到正常区间后再次进入才重新提醒，冷却期作为兜底。
- 当前即时通知只检查 `1d_est`，普通 `1d` 和 `10m` 只展示。

`1d_est` 是盘中估算，不是正式收盘确认信号。中航光电个股计划、烽火通信长期持有、沪深300 ETF 规则是不同策略，不能只因它们都使用 KDJ 就混用参数。

### 收盘总结与次日指引

- 14:50 后发送当日 KDJ 总结。
- 15:15 后先强制刷新持仓标的正式日线；所有信号日期必须等于当天，否则不发送并持续重试，禁止用旧日线充数。
- 当天日线齐备后先发送确定性主计划并记录幂等；`use_llm_advice: true` 时再用Codex做只读复核，Codex失败才回退Axera，两边都失败则不发送模型复核且不影响主计划。
- 模型复核固定输出一致性、主要风险、纪律提醒和人工复核标志；不符合JSON结构的结果一律视为失败。微信复核消息会明确标注“Codex”或“Axera备用”来源。
- A 股 T+1：当天买入的股票，下一交易日可以卖；当天不能卖。
- 每笔手续费按 `lots × fee_per_lot` 保存，完整 T 收益必须扣除买卖两边费用。
- `average_entry_cost` 表示仍持有股票的平均买入成本；`breakeven_cost` 表示扣除已实现卖出收益后的保本成本，两者禁止混用。
- 页面“持仓收益”按`breakeven_cost`/净投入计算，以对齐券商当前成本；`inventory_return`保留按在手股票平均买入成本计算的库存收益。

## 五、研究能力与适用范围

### KDJ 回测/寻优

- 规则：K 小于买入阈值时产生信号，下一根 K 线开盘全仓买入；K 大于卖出阈值时下一根开盘卖出。
- 成本假设：0.05% 手续费和 0.1% 滑点。
- 寻优网格：buy ∈ {5,10,15,20,25}，sell ∈ {75,80,85,90,95}。
- 合格约束：完整往返不少于 8 次，最大回撤不低于 -45%；按样本内总收益选优。

### 价格波段分析

`band_analysis.py` 对每日最低价/最高价判断是否触及 B/S，并假设按阈值成交。该模型适合探索价格区间，不应直接当成可复现实盘收益，因为同一天高低价的先后顺序未知，且最优 B/S 是样本内搜索结果。

### Walk-forward与影子验证

- Walk-forward每折只用过去训练期选参数，再到随后年份测试；事件按信号日之前的数据分为上升、下降、震荡和高波动状态。
- 当前规则的33个样本外事件中，震荡状态9次、胜率44.44%、30日平均收益0.78%；高波动状态11次、胜率72.73%、平均收益3.92%。分组样本仍小，只用于提出验证假设。
- 影子层每天冻结一次正式计划，按5/10/20/30/60交易日评分。“持有”只评价没有新增仓位的机会成本；已有核心仓收益不计入该动作评分。
- 限价买入只有次日开盘明确落在允许区间才记为成交；OHLC无法确认顺序时标记不确定，不虚构成交。

### 分阶段资金曲线

当前规则的33个样本外事件经去重后得到25个不重叠30日事件，并按20万元完整资金池、85%部署上限比较固定手数。结果只衡量“超卖确认事件交易”，不等同于长期持有核心仓，也不用于自动套用参数：

| 手数 | 执行/跳过事件 | 累计资金池收益 | 年化资金池收益 | 最大回撤 |
| --- | --- | --- | --- | --- |
| 10 | 25/0 | 17.45% | 1.54% | -8.29% |
| 15 | 25/0 | 26.18% | 2.24% | -11.89% |
| 20 | 25/0 | 34.90% | 2.89% | -15.18% |
| 30 | 20/5 | 11.68% | 1.06% | -14.69% |
| 40 | 17/8 | 13.10% | 1.18% | -19.50% |

这组结果说明当前“只等待超卖确认”的规则本身不足以支持20%–30%资金池年化目标；扩大到30/40手还会因资金上限跳过高价事件，不能靠加手数解决收益目标。当前10→15手晋级未通过：盈利测试窗口比例、30日影子样本、影子质量和基本面闸门仍不合格。

### 沪深300交易准则

`TRADING_RULES.md` 是沪深300指数/ETF 的独立研究结论：50% 底仓 + 50% 机动仓，收盘确认后 K<10 买、K>85 卖。它不是中航光电或其他个股的默认操作规则。

## 六、当前已知问题

1. 影子记录从2026-08-25开始，真实到期样本不足；不足10个30日样本前不允许阶段晋级，不足30条总记录前不修改参数。
2. 2026年交易所休市日已内置；跨到新年份前必须按官方年度安排更新，未知年份默认停止定时任务并在运行状态中显示未核验。
3. 写接口已有令牌保护，但查询接口仍包含持仓数据且没有HTTPS，只能部署在可信局域网/防火墙后。
4. 中航确认规则已有样本外与资金曲线研究；原KDJ全仓阈值寻优、价格B/S分析仍属于样本内探索，不能覆盖确定性主计划。
5. 外部行情、SMTP、Pushplus和LLM的真实网络故障无法完全由单元测试模拟，需结合持久化任务状态和日志监控。
6. 两个本地迁移压缩包可能包含私有配置，已保持未跟踪状态，不应提交或放到开放目录。

## 七、后续开发顺序

工程功能阶段已经收尾，后续转为按时间积累证据和年度维护：

1. 每个正式交易日自动积累并评分影子记录；系统自行展示到期数量，不提前用未成熟样本下结论。
2. 最新定期报告复核为`pass`、滚动窗口稳定性和30日影子门槛全部通过后，才允许从10手评估晋级15手。
3. 每年交易所公布下一年度休市安排后更新`trading_calendar.py`并增加对应节假日测试。
4. 定期备份`config.yaml`与`runtime/`，轮换写入令牌；查询页面继续限制在可信局域网。
5. 额外买入型T仓、30/40手和其他新策略仍保持关闭；用户明确启用的反T只动用现有可卖老仓20%额度，单次1手且必须先完成回补。

## 八、开发约定

- 不自动下单，除非用户以后明确改变项目边界。
- 给交易建议前必须先核对最新成交和真实持仓。
- 重要修改追加到本文“修改记录”，README 不保存流水账。
- 密钥只走环境变量；不得提交 `.env`、`config.yaml`、缓存、日志和寻优产物。
- 策略或账本修改必须同步增加测试。
- 不把盘中 `1d_est` 描述成正式收盘信号。

## 九、修改记录

### 2026-08-28：独立实时到价提醒

- 新增独立实时到价提醒，不依赖KDJ，也不使用缓存K线充当当前行情。
- 到价区间采用进入一次提醒和离开后重新布防，避免阈值附近重复推送；可配置仅在空仓时提醒。

### 2026-08-27：关闭反T保护性高价回补

- 卖后继续上涨时不高价追回；允许暂时少持1手，避免把反T做成低卖高买。
- 当前待补回的1手只保留34.36元盈利回价，上涨不触发回补；回补前仍禁止新一轮反T。
- 下文的保护性回补是初版历史记录，已被本条规则废止，不参与当前执行。

### 2026-08-26：反T卖出条件简化，趋势过滤转为备用

- 当前只用两个市场条件：价格较前收冲高约1.8%，10分钟K从80以上拐头；不再要求用户理解或等待MA20/MA60。
- 原趋势判断代码和参数完整保留，但由`trend_filter_enabled: false`关闭，不参与当前卖出判断；以后经用户明确确认可重新开启并与简单条件组合。
- 仓位/T+1检查仍在后台强制执行：单次及单日1手、至少保留8手、上一轮未补回不开下一轮。

### 2026-08-26：反T盈利价差调整为1.8%

- 用户确认反T盈利回补目标采用实际卖出价下方约1.8%，替换原0.30元/0.25ATR盈利回补算法。
- 补回限价向下取到分，保证实际价差不因四舍五入低于1.8%；PushPlus、确定性指引和Web同步显示目标百分比。
- 卖出信号、20%额度、单次1手和核心仓底线不变；保护性回补后续已关闭。

### 2026-08-26：中航10分钟价格/K值联动

- `002179` 10分钟价格图在每根K线上方直接标注对应K值。
- 图表顶部增加联动数据条，鼠标悬停、键盘聚焦或手机点按后显示对应时间、开高低收和K/D/J，默认显示最新一根。

### 2026-08-26：反T闭环初版（趋势过滤现已转为备用）

- 用户确认5手阶段已完成两轮成功反T，现有成交合计毛价差109元、四笔手续费20元、净收益约89元。
- 新增20%反T额度：当前10手对应最多2手额度，单次及单日只卖1手可卖老仓，至少保留8手核心仓。
- 初版曾要求MA趋势和10分钟K共同通过；该趋势条件现已由后续修改转为关闭的备用开关。
- 实际卖出录入后，账本保存待补回手数和卖出参考价，生成盈利回补与向上突破保护性回补价；补回前禁止新一轮。
- API、Web、收盘指引和PushPlus增加独立反T结论；完整信号参数随提醒持久化，供后续真实/影子复盘。
- 自动化测试现为47项，覆盖20%额度、单次1手、简单冲高规则、备用趋势开关、盈利回补、禁止高价追回、单日一轮限制、主计划回补价对齐和未成交挂单展示。

### 2026-08-26：工程收尾第五阶段

- 新增固定手数样本外资金曲线，将33个事件去重为25个不重叠事件，对比10/15/20/30/40手在20万元资金池和85%部署上限下的收益与回撤。
- 新增阶段晋级闸门；当前10→15手因窗口稳定性、真实影子样本和基本面状态不足而保持关闭，报告绝不自动修改实盘仓位。
- 提醒历史、区间/冷却和每日通知分通道结果持久化；服务重启后成功通道不会重复发送，09:00后健康检查路径可达。
- 接入2026年交易所官方休市安排，未知年份默认停止定时任务；新增运行状态和下一交易日查询。
- 所有写接口增加本地令牌保护；Web增加令牌保存、成交ID加载和安全纠错，账本校验通过后才替换最终事实，审计不保存错误值。
- 删除无调用方的旧版百分比T计划模板，只保留确定性决策引擎；核心自动化测试增加到35项。

### 2026-08-26：影子评分与状态分层第四阶段

- 新增正式计划影子留痕：同一信号日与引擎版本只保存首次计划，后续不回写历史决策。
- 自动在5/10/20/30/60个交易日后评分；持有只衡量增量机会成本，买入对跳空及日内路径采用保守成交判定。
- Walk-forward事件新增无前视价格状态标签和分组统计；当前分组样本较少，不触发参数变更。
- 行情入口新增盘中当日日线过滤，净值回填可剔除未收盘快照，防止影子评分与净值出现前视污染。
- Web/API新增影子记录、到期进度和状态分层展示；核心自动化测试增加到23项。

### 2026-08-25：文档整合

- 按当前代码和运行态重写 README 与项目总纲，补齐 8 月新增的收盘总结、人工成交、价格波段分析和 LLM 次日指引。
- 明确动态交易状态归属、状态持久化边界、当前已知问题和后续开发顺序。
- 修正端口、观察池、提醒对象和 T+1 等过时表述。
- 将持久化交易记忆压缩为权威摘要和稳定偏好，删除旧快照、重复规则及纠错过程；移除 LLM 提示词中的硬编码旧持仓示例，改为传入当前事实和真实流水。

### 2026-08-25：可信交易账本第一阶段

- 新增 `trade_ledger`，由确认起始仓位和成交事实重算核心仓、T仓、待补回数量、平均买入成本、保本成本、已实现收益及当日可卖手数。
- 成交写入前执行持仓和T+1校验；Web成交表单新增核心仓/T仓选择，成交价改为必填。
- `/api/status` 和 `/api/trade-ledger` 暴露确定性账本，LLM建议改为优先使用正式收盘日线和账本结果。
- 用中航光电现有8笔流水增加回归测试，并覆盖次交易日解锁、T仓分离和同日买卖拒绝。

### 2026-08-25：确定性决策引擎第二阶段

- 新增正式日线驱动的结构化决策引擎，按账本、基本面、回撤、资金、阶段仓位、超卖与止跌确认逐项放行。
- 固定输出动作、状态、最多手数、执行区间、操作后仓位和取消条件；长期仓不会套用中航规则，额外买入型T仓默认关闭，中航反T按20%老仓额度单独控制。
- Web新增“确定性交易计划”卡片，API新增 `/api/decision-plan`。
- 次日通知先发送确定性主计划，再单独调用LLM做只读复核；模型失败不再阻塞主计划。
- 决策与账本自动化测试增加到11项，并加入正式日线过期保护。

### 2026-08-26：净值与样本外验证第三阶段

- 新增20万元策略资金每日净值快照，从真实起始仓位和成交回放现金、市值、高水位、当前/最大回撤；同日运行按日期覆盖。
- 决策引擎改为优先读取持久化高水位，不再只使用配置中的静态初始值。
- 新增81组确认参数、扩展训练/后续测试的walk-forward事件研究，信号收盘确认、下一交易日开盘成交，计双边费用和滑点。
- 当前规则样本外33个独立事件，30日胜率63.64%、平均收益2.02%，但10%分位-10.27%、最差-17.60%、最深期间下探-27.03%。
- 滚动选优仅13个样本且测试窗口盈利比例50%，未达到影子验证门槛；不自动修改核心扩仓参数，不开启额外买入型T仓。
- Web与API新增策略净值及样本外研究展示；自动化测试增加到15项。

### 2026-08-24～2026-08-25

- 接入 LLM 次日交易建议、重试和健康检查；次日计划从15:15起使用主源、独立备用源刷新并严格校验当天正式日线。
- LLM 失败时不再回退旧模板。
- 加入工程外交易记忆读取。

### 2026-08-07～2026-08-14

- 新增中航光电价格波段 B/S 搜索与明细。
- 新增人工成交录入、持仓流水和次日 T+1 操作指引。
- 增加交易记忆，要求指引结合真实成交。

### 2026-08-03

- 新增 14:50 KDJ 收盘前总结。

### 2026-07-24～2026-07-30

- 完成沪深300 KDJ 回测与交易准则。
- 新增 `1d_est`、Pushplus、多标的、个股阈值寻优、服务内回测、行情缓存和 systemd 部署。
- 密钥迁移到 `.env`，配置和运行产物移出 Git。

### 2026-06-24

- 完成第一版 FastAPI、AkShare 行情、KDJ 阈值提醒和局域网页面。
